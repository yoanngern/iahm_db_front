'use strict';

describe('iahmDBApp.searchView module', function() {

  beforeEach(module('iahmDBApp.searchView'));

  describe('searchView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('searchViewCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});