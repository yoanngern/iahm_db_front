'use strict';

describe('iahmDBApp.contactView module', function() {

  beforeEach(module('iahmDBApp.contactView'));

  describe('contactView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('contactCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});